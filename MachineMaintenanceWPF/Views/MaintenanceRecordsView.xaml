<UserControl x:Class="MachineMaintenanceWPF.Views.MaintenanceRecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1300"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🔧 Maintenance Records" FontSize="32" FontWeight="Bold" 
                      Foreground="#2c3e50" Margin="0,0,0,10"/>
            <TextBlock Text="View and manage all machine maintenance activities" 
                      FontSize="16" Foreground="#7f8c8d"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Records}" 
                 AutoGenerateColumns="False" CanUserAddRows="False" 
                 IsReadOnly="True" AlternatingRowBackground="#f5f5f5"
                 GridLinesVisibility="Horizontal" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Machine ID" Binding="{Binding MachineId}" Width="Auto"/>
                <DataGridTextColumn Header="Machine Name" Binding="{Binding MachineName}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding MaintenanceType}" Width="Auto"/>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="Auto"/>
                <DataGridTextColumn Header="Scheduled Date" Binding="{Binding ScheduledDate, StringFormat='yyyy-MM-dd'}" Width="Auto"/>
                <DataGridTextColumn Header="Technician" Binding="{Binding Technician}" Width="Auto"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
